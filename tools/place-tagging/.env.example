# 이 파일을 복사해 같은 폴더에 .env 로 저장하고 값을 채운다. .env 는 git에 올라가지 않는다.
GMS_API_KEY=
GMS_API_KEY_2=
GEMINI_CHAT_MODEL=gemini-2.5-flash-lite
# 콘솔에서 확인한 하루 요청 한도를 알면 숫자로 넣는다(비우면 429 응답으로 판단)
GMS_DAILY_REQUEST_LIMIT=
